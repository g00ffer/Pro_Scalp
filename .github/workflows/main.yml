    - name: Install dependencies and pytest
      run: |
        python -m pip install --upgrade pip
        pip install .[test] || pip install . # Установит проект и зависимости из pyproject.toml
        pip install pytest

    - name: Run tests with pytest
      run: |
        pytest
